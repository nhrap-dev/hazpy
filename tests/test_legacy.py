import unittest

class TestLegacy(unittest.TestCase):

    def testHazusDBInit(self):
      # requires connection to hazus database
      #   from hazpy.legacy import HazusDB
      #   db = HazusDB()
      #   self.assertEqual(type(db), type(HazusDB()))
      pass
   

